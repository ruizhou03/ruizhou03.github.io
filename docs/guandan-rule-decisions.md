# 掼蛋规则决策记录

## 2026-07-23 · `gd-huaian-2025-site-v1`

状态：已采纳。

1. 过 A 采用 1+2、1+3 可过，1+4 不可过。这样胜负条件与升级幅度直接对应。
2. 双下抗贡计算贡方两人的大王总数。两张大王可分居两人之手。
3. 同队进贡默认关闭，只保留显式娱乐变体开关。
4. 还贡限制按牌面数字而非本局牌力判断；2 至 10 的数字级牌可还。
5. 万能牌歧义按出牌上下文解释。领出保持确定性默认，跟牌优先解释为桌面牌型。
6. 首局随机首家、超时托管和炸弹积分被标记为本站数字适配。
7. 旧无版本存档迁移到 schema 2；未知规则版本与未来 schema 拒绝，避免跨规则静默续局。
8. 历史模拟结果若未声明该规则版本，不能继续作为当前 AI 强度产品证据。

机器可执行示例由 `tests/fixtures/guandan-rules-golden.json` 固定。后续改变上述任何一项时，必须创建新的 `rulesVersion`、更新 fixture、迁移策略与发布说明。
